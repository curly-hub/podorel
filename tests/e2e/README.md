# E2E Tests

This directory is reserved for browser-based end-to-end tests. Current automated
coverage lives in Go tests, fake-agent integration tests, UI scaffold checks,
Angular builds, and deployment dry runs through `scripts/test-all.sh`.
